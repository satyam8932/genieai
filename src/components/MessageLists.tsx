import React from 'react'
import { Message } from 'ai/react'

type Props = {
    message: Message[]
}

const MessageLists = (props: Props) => {
  return (
    <div>MessageLists</div>
  )
}

export default MessageLists


// Message List needs to be created